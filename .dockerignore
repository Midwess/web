# Build artifacts (we rebuild inside the image)
node_modules
dist
.vite
.cache

# Editor / OS
.DS_Store
.idea
.vscode

# VCS metadata is included because the build resolves the pinned public
# submodule commit. The final nginx image never receives it.
.gitignore

# Local config
.openclaude
.claude

# Editor / dev artifacts
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Tests
coverage
.nyc_output

# Misc
.eslintcache
.env
.env.local
.env.*.local

# Generated / cache
.turbo
.next
.output
.swc
