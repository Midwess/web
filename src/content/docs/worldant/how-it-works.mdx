---
title: How it works
description: The target execution model for the Worldant world runtime.
---

Worldant turns a directory into a running world.

```text
agent edits files
  -> worldant build
  -> validate migrations, artifacts, contracts, tests, and policies
  -> write .dist/run/<run_snapshot_id>
  -> record snapshot metadata in PgPaw
  -> worldant run serves latest valid snapshots
```

Execution never runs directly from the working tree. New calls use the newest
valid snapshot. In-flight workflow runs stay pinned to the snapshot they started
with.

## Database truth

PgPaw/Postgres is the truth.

```text
if it did not commit to PgPaw, it did not happen
if it committed to PgPaw, subscribers can observe it
```

Postgres row-level policy is the authorization boundary. Worldant can validate
capability declarations, but database policy remains the final guard.

## Execution

Worldant plans bounded JavaScript isolate slots using `deno_core::JsRuntime`.
Commands, mutations, workflow replays, and steps load from `.dist` on demand.
Idle entries and isolate slots are dropped to keep per-user infrastructure small.

Durable workflow replay uses a deterministic `node:vm`-style context inside an
isolate slot. Steps execute outside that deterministic context and persist their
results before workflow replay continues.
