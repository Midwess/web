---
title: Worldant
description: Active design and implementation notes for the Worldant world runtime.
---

Worldant is an actively in-progress runtime for AI-owned software worlds.

It is being rebuilt around a directory-first model: an agent edits files, the
daemon validates and builds a `.dist` snapshot, and the running world exposes
Commands and durable Workflows.

This documentation describes the target architecture. APIs are not stable yet.
The public distribution, installer, and packaging live at
[github.com/Midwess/worldant](https://github.com/Midwess/worldant).

## Why it exists

AI should be able to expand its ability by creating real software capability:
schema, logic, durable jobs, reactive views, and typed interfaces. The result
must still be normal software. It must have a database truth, authorization,
crash recovery, deploy safety, and a way for humans and generated UI to use the
same surface.

Worldant is that runtime boundary.

## What Worldant owns

- World directory discovery and validation.
- SQL migration validation and schema reflection.
- PgPaw/Postgres as database truth and realtime data plane.
- Commands for immediate reads and writes (there is no separate Mutation kind).
- Durable Workflows with internal, replay-safe Steps.
- Collection synchronization and event streams as protocol facilities.
- HTTP surfaces for outside agents.

## Core rule

```text
All durable truth enters PgPaw/Postgres.
All visible clients subscribe back from PgPaw/Postgres.
```

Commands may compute. Mutations may commit. Workflows may wait and resume.
Reactive streams may notify UI, agents, and other consumers. None of those paths
becomes a hidden state system outside the database.

## Shape

```text
world/
  worldant.ts
  apps/
    <app>/
      migrations/
      commands/
      workflows/
        steps/
      tests/
  generated/

worldant build
  -> .dist/run/<run_snapshot_id>
  -> PgPaw metadata
  -> public catalog metadata

worldant run
  -> daemon serves latest valid snapshots
  -> workflow runs pin to their snapshot
```

## Status

Worldant is actively in progress. The current proof target is:

- A generated chess world with schema, mutation, command, workflow, and reactive
  board subscription.
- A live az-wire endpoint that exposes the world as typed functions.
- A Ragent UI that imports `worldant-client` and interacts through committed
  database truth.
- A long-running workflow that survives restart and resumes from PgPaw state.

See the repository `thesis.md` for the current design details.
